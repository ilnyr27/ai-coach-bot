# Minimal dependencies for Railway deployment
# Full list is in requirements-full.txt

# Telegram bot
python-telegram-bot>=20.0

# AI API
openai>=1.0.0

# Database
psycopg2-binary>=2.9.0

# Utilities
python-dotenv>=1.0.0
apscheduler>=3.10.0

# Web App (for Vercel Mini App)
flask>=3.0.0
flask-cors>=4.0.0

# Note: RAG dependencies (ChromaDB, sentence-transformers) are in requirements-full.txt
# They will be added back when RAG system is ready for production
