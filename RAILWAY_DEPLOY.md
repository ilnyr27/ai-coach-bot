# Деплой на Railway - Пошаговая инструкция

## ✅ Готовые файлы:
- `Procfile` - команда запуска бота
- `runtime.txt` - версия Python
- `railway.json` - конфигурация Railway
- `requirements.txt` - зависимости Python

---

## 🚀 Шаг 1: Создать GitHub репозиторий

### Вариант A: Если репозитория нет

1. Открой https://github.com/new
2. Название: `ai-coach-bot`
3. Приватность: Private
4. Create repository

На компьютере:
```bash
cd C:\Users\ilray\Claude\Lichnosti
git init
git add .
git commit -m "Initial commit - AI Coach Bot for Railway"
git branch -M main
git remote add origin https://github.com/твой-username/ai-coach-bot.git
git push -u origin main
```

### Вариант B: Если репозиторий уже есть

```bash
cd C:\Users\ilray\Claude\Lichnosti
git add .
git commit -m "Add Railway deployment config"
git push
```

---

## 🚄 Шаг 2: Railway Setup

### 1. Создать аккаунт на Railway

1. Открой https://railway.app
2. Нажми **"Start a New Project"**
3. Войди через GitHub

### 2. Создать новый проект

1. Нажми **"New Project"**
2. Выбери **"Deploy from GitHub repo"**
3. Выбери репозиторий `ai-coach-bot`
4. Railway автоматически обнаружит `Procfile`

---

## 🗄️ Шаг 3: Добавить PostgreSQL

1. В Railway проекте нажми **"+ New"**
2. Выбери **"Database"** → **"Add PostgreSQL"**
3. PostgreSQL создастся автоматически
4. Railway сгенерирует `DATABASE_URL` переменную

---

## ⚙️ Шаг 4: Настроить Environment Variables

В Railway проекте:

1. Выбери сервис бота (не PostgreSQL)
2. Перейди в **"Variables"**
3. Добавь переменные:

```
TELEGRAM_BOT_TOKEN=8471169770:AAEY2w9gGS1eh-x-XlVZlHuiulaf2wXgEgs
DEEPSEEK_API_KEY=sk-fafd2548b0e74dec951e77a7197a085a
```

**НЕ НУЖНО добавлять DATABASE_URL** - Railway автоматически подключит PostgreSQL!

---

## 🚀 Шаг 5: Deploy!

1. Railway автоматически задеплоит при push в GitHub
2. Или нажми **"Deploy"** вручную

Жди 2-3 минуты пока:
- Установятся зависимости
- Запустится бот

---

## 📊 Шаг 6: Проверка

### Смотреть логи:

1. В Railway выбери сервис бота
2. Перейди в **"Deployments"**
3. Кликни на последний деплой
4. Смотри логи в реальном времени

**Должно быть:**
```
✅ Bot components initialized!
✅ Scheduler started with bot integration
✅ Bot is running!
```

### Тестировать в Telegram:

1. Открой бота в Telegram
2. Напиши `/start`
3. Попробуй добавить цель
4. Проверь что всё работает

---

## 🎯 Шаг 7: Инициализация БД (опционально)

Если нужно создать таблицы вручную:

1. В Railway выбери PostgreSQL
2. Перейди в **"Data"** → **"Query"**
3. Запусти SQL из `scripts/postgres_schema.sql`

Но обычно `db_manager_postgres.py` создаст таблицы автоматически!

---

## 🔄 Автообновление

Теперь при каждом `git push`:
1. Railway автоматически задеплоит
2. Бот перезапустится
3. Изменения применятся

---

## 💡 Полезные команды

### Локально тестировать перед push:

```bash
# Проверить что бот запускается
python src/bot/proactive_bot.py

# Если норм - закоммитить
git add .
git commit -m "Your changes"
git push
```

### Посмотреть статус на Railway:

- **Deployments** - история деплоев
- **Metrics** - CPU, память, трафик
- **Settings** - настройки

---

## 🎉 Готово!

Теперь у тебя:
- ✅ Production-ready бот на Railway
- ✅ PostgreSQL база данных
- ✅ Автодеплой из GitHub
- ✅ Масштабируемость до 100,000+ пользователей
- ✅ Профессиональные логи

**Бот работает 24/7 с автоперезапуском!**

---

## 📈 Мониторинг

Railway показывает:
- CPU usage
- Memory usage
- Request count
- Логи в реальном времени

---

## 💰 Стоимость

**Free tier:**
- $5 кредитов в месяц
- Хватит на ~500 часов работы
- Для начала достаточно

**Paid tier (если нужно):**
- $5/месяц за дополнительные ресурсы

---

## 🆘 Если что-то не работает

### Бот не запускается:

1. Проверь логи в Railway
2. Убедись что `Procfile` правильный
3. Проверь environment variables

### БД не подключается:

1. Проверь что PostgreSQL сервис создан
2. Railway должен автоматически добавить `DATABASE_URL`
3. Посмотри в Variables сервиса бота

### Нужна помощь:

- Railway Discord: https://discord.gg/railway
- Документация: https://docs.railway.app
